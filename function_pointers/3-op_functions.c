#include <stdio.h>
/**
 *op_add- add to ints
 *@a: holds an int
 *@b: holds an int
 *Return: the result of the operation
 */
int op_add(int a, int b)
{
	return (a + b);
} /*end add*/
/**
 *op_sub- calculates the diference between 2 ints
 *@a: holds an int
 *@b: holds an int
 *Return: the result of the operation
 */
int op_sub(int a, int b)
{
	return (a - b);
} /*end sub*/
/**
 *op_mul- calculates the mult between 2 ints
 *@a: holds an int
 *@b: holds an int
 *Return: the result of the operation
 */
int op_mul(int a, int b)
{
	return (a * b);
} /*end mul*/
/**
 *op_div- calculates thr div between 2 ints
 *@a: holds an int
 *@b: holds an int
 *Return: the result of the operation
 */
int op_div(int a, int b)
{
	return (a / b);
} /*end div*/
/**
 *op_mod- caulculate sthe mood betwee 2 ints
 *@a: holds an int
 *@b: hoolds an int
 *Return: the result of the opertion
 */
int op_mod(int a, int b)
{
	return (a % b);
} /*end mod*/

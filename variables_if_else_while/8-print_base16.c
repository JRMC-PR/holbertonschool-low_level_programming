#include <stdio.h>
/**
 *main- Execute
 *Return: 0 if success
 *
 *
 */
int mani(void)
{
	int i;
	char a;

	for (i = 48 ; i <58 ; i++)
		putchat(i);

	for (a = 'a' ; a <= 'f' ; a++)
		putchar(a);

	return (0);


}

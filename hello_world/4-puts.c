#include <stdio.h>
/*
 *Main: Executes the program
 *
 *Return: if ok is 0
 */
int main(void)
{
	char output[] = "\"Programming is like building a multilingual puzzle";

	puts(output);

	return (0);
}
